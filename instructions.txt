create me a secure secrets vault service.  This service should function similarly to hashicorp vault.  Include a java and a go client.  Include tests that use these clients.  The service should be written in go. A client should authenticate along with a policy id.  this policy id should determine which secrets they have access to and what type of access (read, write, delete).  Secrets should also support a simple versioning scheme.  The client should be able to ask for a specific version or be given the latest version. Include a readme.md file that describes the design and operation of the service including how to build.  Include documentation on how to build and use the clients including how incorporate the client in your own code.  The server should support multiple nodes and a way to replicate secrets between nodes.
